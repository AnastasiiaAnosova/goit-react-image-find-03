import styled from 'styled-components';

export const LoaderStyles = styled.div`
  position: fixed;
  bottom: 0;
  left: 50%;
  top: 45%;
  transform: translateX(-50%);
  z-index: 9999; // Збільште значення zIndex, якщо потрібно
  padding: 10px; // Додайте потрібний відступ
  
`